# Frontend configuration for EPAGAL Tesis
# Copy this to .env.local and update with your values

# Backend API URL (FastAPI from Andrea's repository)
# Example: https://epagal-backend-routing-latest.onrender.com
# The /api path will be added automatically if not present
REACT_APP_API_URL=https://epagal-backend-routing-latest.onrender.com

# Optional: Node environment
NODE_ENV=production
