services:
  # React Frontend (consume backend Go en Render)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: residuos_frontend
    ports:
      - "3000:3000"
    restart: unless-stopped

networks:
  residuos_network:
    driver: bridge

volumes: {}